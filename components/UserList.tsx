import React from 'react'

function UserList() {
  return (
    <div>
       {/* {userList.map((user, index) => (
            <li key={index}>
                {user.name} ({user.age})
            </li>
        ))} */}
    </div>
  )
}

export default UserList
